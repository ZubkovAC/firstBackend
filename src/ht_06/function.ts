export const pageNumber = (pageNum :string) => pageNum ? +pageNum : 1
export const pageSize = (pageSiz :string) => pageSiz ? +pageSiz : 10
export const searchNameTerm = (searchName :string) => searchName ? searchName : ''